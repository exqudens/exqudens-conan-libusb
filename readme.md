# exqudens-conan-libusb

##### How To Build

```
git clean -xdf
cmake --preset <name>
cmake --build --preset <name>
```

##### How To Export

```
git clean -xdf
cmake --preset <name>
cmake --build --preset <name> --target conan-export
```
